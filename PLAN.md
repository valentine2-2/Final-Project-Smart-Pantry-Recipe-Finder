# 📋 PLAN.md: Software Requirement Specification & Development Plan

**โปรเจกต์:** Smart Pantry & Recipe Finder (CLI Version)  
**ทีมงานและบทบาท:**
- 🏗️ **Planner (Architect):** ไทน์ (วางโครงสร้างสเปกต์ ออกแบบ Data Flow และ Logic)
- 🔨 **Coders (Builders):** ฟ่า & ภีม (ลงมือพัฒนาฟังก์ชันหลักและส่วนประมวลผล)
- 🐞 **Debugger (Finisher):** โดนัท (ทดสอบ Edge Cases ดักจับ Error และสรุปรายงาน)

---

## 📌 Phase 1: Requirements & Scope Definition (วางแผนและกำหนดขอบเขต)

### 1. กำหนดขอบเขตระบบ CLI (System Scope)
ระบบต้องทำงานผ่าน Command Line Interface (CLI) โดยมีฟีเจอร์หลักดังนี้:
1. **แสดงข้อความต้อนรับ:** แสดงแบนเนอร์ระบบ Smart Pantry & Recipe Finder
2. **รับคำสั่งจากผู้ใช้:** รองรับการเลือกเมนู (`add`, `view`, `expiring`, `recipe`, `quit`)
3. **จัดการคลังวัตถุดิบ (Pantry Management):** รับชื่อวัตถุดิบ จำนวน หน่วยนับ และวันหมดอายุ
4. **ตรวจสอบวันหมดอายุ (Expiry Alert):** กรองและแจ้งเตือนวัตถุดิบที่กำลังจะหมดอายุภายใน 3 วัน
5. **วิเคราะห์เมนูและ Shopping List:** จับคู่คลังวัตถุดิบกับเมนูอาหาร พร้อมคำนวณของที่ต้องซื้อเพิ่ม
6. **ออกจากโปรแกรม:** ปิดการทำงานได้อย่างปลอดภัยเมื่อรับคำสั่งออกจากระบบ

### 2. นิยาม Definition of Done (DoD)
โปรแกรมจะถือว่าเสร็จสมบูรณ์พร้อมส่งมอบเมื่อผ่านเงื่อนไขต่อไปนี้:
* [x] การพิมพ์คำสั่ง `quit` ไม่ว่าจะพิมพ์ตัวเล็ก ตัวใหญ่ หรือมีช่องว่าง (เช่น `quit`, `QUIT`, ` quit `) ต้องออกจากโปรแกรมทันที
* [ ] การกรอกจำนวนวัตถุดิบที่ไม่ใช่ตัวเลข หรือเลขติดลบ ต้องถูกดักจับด้วย `try-except` และแสดงข้อความเตือนโดยโปรแกรมไม่ Crash
* [ ] การคำนวณวันหมดอายุและการจับคู่เมนูอาหาร (Matched / Missing Ingredients) ต้องประมวลผลถูกต้องตาม Logic
* [ ] โค้ดมีการแยกฟังก์ชันชัดเจน มี Docstring กำกับทุกฟังก์ชัน และใช้ตัวแปรสื่อความหมายตามสไตล์ Python

### 3. ภารกิจ Coder / Debugger Mission
* ก่อนเริ่มเขียนโค้ด ทีม Builders (ฟ่า & ภีม) และ Finisher (โดนัท) ต้องประชุมทบทวนเอกสาร `PLAN.md` ฉบับนี้ร่วมกับ Architect (ไทน์) เพื่อเคลียร์ข้อสงสัยของ Logic ให้เข้าใจตรงกัน 100%

---

## 🏗️ Phase 2: Execution (ลงมือพัฒนา — Mid-Week Development)

### โครงสร้างฟังก์ชันหลักที่ต้องส่งมอบ

#### `display_welcome_message()`
* **หน้าที่:** แสดงแบนเนอร์ข้อความต้อนรับเข้าสู่ระบบ Smart Pantry & Recipe Finder
* **Input:** ไม่มี
* **Output:** พิมพ์แบนเนอร์ข้อความสวยงามออกทางหน้าจอ Terminal

#### `get_command_input()`
* **หน้าที่:** รับค่าคำสั่งจากผู้ใช้ผ่าน Keyboard
* **Processing:** ตัดช่องว่างด้วย `.strip()` และแปลงเป็นตัวพิมพ์เล็กด้วย `.lower()`
* **Return:** `str` (คำสั่งที่คลีนแล้ว)

#### `add_pantry_item(pantry)`
* **หน้าที่:** รับชื่อวัตถุดิบ จำนวน หน่วยนับ และวันหมดอายุ เพื่อบันทึกลง Dictionary
* **Processing:** ดักจับ `ValueError` หากผู้ใช้ใส่จำนวนที่ไม่ใช่ตัวเลข หรือรูปแบบวันที่ผิด

#### `view_pantry(pantry)`
* **หน้าที่:** แสดงรายการวัตถุดิบทั้งหมดที่มีในคลัง

#### `check_expiring_items(pantry, days=3)`
* **หน้าที่:** กรองวัตถุดิบที่มีวันหมดอายุเหลือไม่เกิน `days` วันที่กำหนด แล้วแสดงผลแจ้งเตือน

#### `recommend_recipes(pantry)`
* **หน้าที่:** เปรียบเทียบวัตถุดิบในคลังกับฐานข้อมูลเมนูอาหาร แสดงรายการที่ทำได้ และสร้าง Shopping List สำหรับวัตถุดิบที่ขาด

#### `main()`
* **หน้าที่:** ควบคุม Flow การทำงานหลักด้วย `while True` loop
* **Processing:** เรียกใช้ `get_command_input()` เพื่อรับคำสั่ง พร้อมครอบโครงสร้าง `try-except` สำหรับดักจับ Exception รวมของระบบ

---

## 🐞 Phase 3: Review & Handoff (ทบทวนและทดสอบ — Friday Review)

### 1. เคสการทดสอบขอบเขต (Edge Case Testing Checklist)

| ชุดทดสอบ | แอ็กชันการทดสอบ (Input) | ผลลัพธ์ที่คาดหวัง (Expected) |
| :--- | :--- | :--- |
| **Command Sensitivity** | พิมพ์ `Quit`, `QUIT`, ` quit ` | จบการทำงานทันที ไม่มองว่าเป็นคำสั่งผิด |
| **Invalid Quantity** | ใส่จำนวนวัตถุดิบเป็น `abc` หรือ `-10` | แจ้งเตือนข้อผิดพลาด ไม่ทำให้โปรแกรมค้าง/Crash |
| **Invalid Date Format** | ใส่ วัน/เดือน/ปี ผิดรูปแบบ เช่น `31/12/2026` (ระบบใช้ `YYYY-MM-DD`) | แจ้งเตือนรูปแบบวันที่ถูกต้อง และให้กรอกใหม่ |
| **Empty Pantry Recipe Match** | กดค้นหาเมนูขณะตู้เย็นยังไม่มีวัตถุดิบเลย | แสดงว่ายังไม่มีวัตถุดิบ และแนะนำรายการที่ต้องซื้อทั้งหมด |

### 2. กระบวนการรายงานข้อผิดพลาด (Bug Report Template)
เมื่อ Debugger (โดนัท) พบข้อผิดพลาด ให้บันทึกรายงานในรูปแบบดังนี้:

```text
[Bug Report]
- สิ่งที่ทำ (Observation): กรอกจำนวนไข่เป็น "สาม" ในเมนู add
- สิ่งที่คาดหวัง (Expected): แสดงเตือน "กรุณากรอกเฉพาะตัวเลข" และให้ป้อนใหม่
- ผลลัพธ์จริง (Actual): เกิด ValueError: could not convert string to float แล้วโปรแกรมหลุดทันที
