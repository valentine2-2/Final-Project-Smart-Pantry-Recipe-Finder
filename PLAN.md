# 📋 PLAN.md: Software Requirement Specification & Development Plan

**โปรเจกต์:** CLI Multiplication Table & Core System  
**ทีมงานและบทบาท:**
- 🏗️ **Planner (Architect):** ไทน์ (วางแผน ออกแบบสเปกต์ และโครงสร้างระบบ)
- 🔨 **Coders (Builders):** ฟ่า & ภีม (ลงมือเขียนโค้ดตามสเปกต์)
- 🐞 **Debugger (Finisher):** โดนัท (ทดสอบระบบ จัดการ Edge Cases และสรุปรายงาน)

---

## 📌 Phase 1: Requirements & Scope Definition (วางแผนและกำหนดขอบเขต)

### 1. ขอบเขตระบบ (System Scope)
โปรแกรมต้องทำงานในรูปแบบ Command Line Interface (CLI) โดยมีขอบเขตการทำงานดังนี้:
1. **แสดงข้อความต้อนรับ:** แสดงแบนเนอร์ระบบเมื่อเริ่มต้นใช้งาน
2. **รับคำสั่งจากผู้ใช้:** รองรับคำสั่งหลักในการทำงาน (พิมพ์สูตรคูณ / ออกจากโปรแกรม)
3. **พิมพ์ตารางสูตรคูณ:** คำนวณและแสดงผลตารางสูตรคูณตามแม่ที่กำหนด และจำนวนแถวที่ต้องการ
4. **ออกจากโปรแกรม:** ปิดการทำงานได้อย่างถูกต้องและปลอดภัยเมื่อผู้ใช้สั่งงาน

### 2. นิยามความสำเร็จ Definition of Done (DoD)
โปรแกรมจะถือว่าเสร็จสมบูรณ์พร้อมส่งมอบเมื่อผ่านเงื่อนไขต่อไปนี้ทั้งหมด:
* [ ] การพิมพ์คำสั่ง `quit` ไม่ว่าจะเขียนเป็นตัวพิมพ์เล็ก ตัวพิมพ์ใหญ่ หรือมีช่องว่าง (เช่น `quit`, `Quit`, `QUIT `, ` QUIT `) ต้องสั่งให้โปรแกรมจบการทำงานทันที
* [ ] การป้อนข้อมูลที่ไม่ใช่ตัวเลขลงในช่องระบุแม่สูตรคูณหรือจำนวนแถว ต้องถูกดักจับด้วย `try-except ValueError` และแสดงข้อความเตือนโดยที่โปรแกรมไม่พัง (Crash)
* [ ] ตารางสูตรคูณต้องแสดงผลถูกต้อง ตั้งแต่ 1 ถึง `end_number` ตามที่ผู้ใช้ระบุ
* [ ] โค้ดมีการแยกฟังก์ชันชัดเจน มี Docstring อธิบายการทำงาน และใช้ชื่อตัวแปรสื่อความหมาย

### 3. ภารกิจประสานงาน (Coder/Debugger Mission)
* ก่อนลงมือเขียนโค้ด ทีม Coder (ฟ่า & ภีม) และ Debugger (โดนัท) ต้องทบทวนเอกสาร `PLAN.md` ฉบับนี้ร่วมกัน และซักถามข้อสงสัยจนเข้าใจตรงกัน 100%

---

## 🏗️ Phase 2: Architecture & Execution (สถาปัตยกรรมและรายละเอียดโค้ด)

### 1. โครงสร้างฟังก์ชันหลักที่ต้องส่งมอบ

#### `display_welcome_message()`
* **หน้าที่:** แสดงแบนเนอร์ข้อความต้อนรับเข้าสู่ระบบ
* **Input:** ไม่มี
* **Output:** พิมพ์ข้อความสวยงามออกทางหน้าจอ Terminal

#### `get_command_input()`
* **หน้าที่:** รับค่าคำสั่งจากผู้ใช้ผ่าน Keyboard
* **Processing:** ใช้ `.strip()` ลบช่องว่างหน้า-หลัง และใช้ `.lower()` แปลงข้อความให้เป็นตัวพิมพ์เล็กทั้งหมด
* **Return:** `str` (ข้อความคำสั่งที่คลีนแล้ว)

#### `display_multiplication_table(number, end_number)`
* **หน้าที่:** คำนวณและแสดงตารางสูตรคูณ
* **Input:** 
  * `number` (int): แม่สูตรคูณที่ต้องการ
  * `end_number` (int): จำนวนแถวสิ้นสุด (เช่น ถึงคูณ 12)
* **Processing:** ใช้ `for` loop วนลูปตั้งแต่ 1 ถึง `end_number` (`range(1, end_number + 1)`)
* **Output:** พิมพ์สูตรคูณแต่ละบรรทัดในรูปแบบ `X x Y = Z`

#### `main()`
* **หน้าที่:** ควบคุม Flow การทำงานหลักของโปรแกรม
* **Processing:** 
  * ใช้ `while True` loop ในการรันโปรแกรมต่อเนื่อง
  * เรียกใช้ `get_command_input()` เพื่อรับคำสั่ง (`table` หรือ `quit`)
  * ครอบด้วย `try-except ValueError` เมื่อรับค่าแม่สูตรคูณและจำนวนแถว เพื่อดักจับข้อมูลที่ไม่ใช่ตัวเลข

### 2. แนวปฏิบัติในการเขียนโค้ด (Coding Standards)
* **Naming Convention:** ใช้ snake_case สำหรับฟังก์ชันและตัวแปร (เช่น `end_number`, `get_command_input`)
* **Docstring:** เขียนคำอธิบายสั้นๆ กำกับหัวฟังก์ชันทุกฟังก์ชัน
* **Conciseness:** กระชับ ไม่ซ้ำซ้อน และอ่านง่าย

---

## 🐞 Phase 3: Review, Testing & Handoff (การทดสอบและส่งมอบงาน)

### 1. เคสการทดสอบขอบเขต (Edge Case Testing Checklist)

| ชุดทดสอบ | รายละเอียดการทดสอบ | ผลลัพธ์ที่คาดหวัง |
| :--- | :--- | :--- |
| **Command Case Sensitivity** | พิมพ์ `Quit`, `QUIT`, ` quit ` | โปรแกรมจบการทำงานทันที ไม่มองว่าเป็นคำสั่งผิดพลาด |
| **Invalid Text Input** | กรอกตัวอักษร (เช่น `abc`, `สอง`) ในช่องแม่สูตรคูณ | แสดงเตือน "กรุณากรอกเฉพาะตัวเลข" และวนกลับไปรับค่าใหม่ |
| **Negative/Zero Input** | กรอกแม่สูตรคูณเป็น 0 หรือติดลบ (เช่น `-5`) | ระบบประมวลผลตามหลักคณิตศาสตร์ได้โดยไม่พัง |
| **Float Input** | กรอกทศนิยม (เช่น `12.5`) | ถูกดักจับด้วย `ValueError` และแจ้งเตือนให้กรอกจำนวนเต็ม |

### 2. แบบฟอร์มรายงานข้อผิดพลาด (Bug Report Template)
เมื่อ Debugger (โดนัท) พบข้อผิดพลาด ให้บันทึกรายงานในรูปแบบต่อไปนี้:

```text
[Bug Report]
- สิ่งที่ทำ (Observation): (ระบุแอ็กชันและ Input ที่ใช้ เช่น พิมพ์ "abc" ในช่องแม่สูตรคูณ)
- สิ่งที่คาดหวัง (Expected): (ระบุพฤติกรรมที่ควรจะเกิดขึ้น เช่น แสดงข้อความเตือนให้ใส่ตัวเลข)
- ผลลัพธ์จริง (Actual): (ระบุ Error หรือพฤติกรรมที่เกิดขึ้น เช่น โปรแกรมขึ้น ValueError Crash)
